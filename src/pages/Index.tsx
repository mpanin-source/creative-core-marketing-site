import RefreshHome from "@/components/cobalt-refresh/RefreshHome";

// Homepage = the integrated Signal-refresh composition: real sections + orbit Six Systems
// + kit patterns (contour / sparkle / monogram) in the coral brand with the blue accent.
const Index = () => <RefreshHome />;

export default Index;
